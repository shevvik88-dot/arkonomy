# Arkonomy — Changelog

Extracted from CLAUDE.md's "Current status" section on 2026-08-19 to keep
the root file short. Full dated project history, verbatim — read on
demand ("what happened when", "why does X work this way", "was this
already fixed"). Newest entries are at the bottom of each run; the file
overall reads oldest → newest.

## Current status (2026-07-13)
- Production live at app.arkonomy.com
- Security audit complete (Items 1–8): CSP headers, session storage hardening, Plaid webhook verification, SW cache audit, logout global scope, Stripe idempotency, input validation, password policy
- **Firebase App Check — DISABLED, not live** (corrected 2026-07-30; this line previously and incorrectly said "live" — caught during a pre-release security audit reading `_shared/appCheck.ts` in isolation without checking call sites). Removed from both frontend and every backend call site in `66ea690` ("revert: remove App Check from all protected edge functions + frontend caller", 2026-07-04) after a Google-side 500 on `exchangeRecaptchaV3Token`. `verifyAppCheck()` in `_shared/appCheck.ts` still exists but nothing imports or calls it; `src/lib/appCheck.js` is orphaned (nothing imports it, not even `main.jsx`); `callEdgeFunction()` does NOT inject an App Check token. ai-chat/get-insights/weekly-report/plaid-sync-transactions currently have no App-Check-based bot/abuse layer. Kept in the tree for future re-enablement — see the "App Check" entries under Coding rules for the design decisions to preserve if/when it comes back.
- **Plaid webhook receiver live** — `supabase/functions/plaid-webhook/` deployed; JWT-verified (ES256, not HMAC); handles TRANSACTIONS/SYNC_UPDATES_AVAILABLE (triggers sync_item) and ITEM/ERROR (sets error_code on plaid_items); register URL in Plaid Dashboard: `https://hvnkxxazjfesbxdkzuba.supabase.co/functions/v1/plaid-webhook`
- `plaid_items.error_code` column added (migration 20260625000000) — populated by webhook on ITEM/ERROR, cleared on successful sync; `check-bank-connection` now returns `error_code` for reconnect prompt UI
- CI: Semgrep + dependency-review GitHub Actions added; Dependabot weekly npm updates enabled
- Security: CSP live on app.arkonomy.com — script/connect/frame/manifest-src all locked down
- Security: signOut uses `scope: 'global'` — invalidates all sessions across devices
- **Recurring-payments staleness filter live** — `computeRecurringSummary` (src/utils/recurringSummary.js) excludes merchants whose last charge exceeds an adaptive threshold (2× typical billing interval, floor 45 days) into `possiblyCancelled`; shown read-only in Insights ("Possibly cancelled?" section), excluded from Subscriptions/Regular Payments totals and ai-chat context (single source, no separate fix needed). Verified against real account data: OpenAI (124d stale) and Dental Insurance (236d stale) correctly flagged; Lemonade Insurance (9d, monthly cadence) correctly stayed active.
- **"Ask about subscription" action live in Insights** — search-icon button on each Subscriptions row opens ai-chat with a neutral prompt ("...is it worth keeping, and how would I cancel it if I wanted to?"), not a direct cancel prompt. Regular Payments has no such action by design (rent/insurance/loans aren't one-click cancellable). `findDuplicateSubscriptions()` renders a "Similar service" badge for 2+ *distinct-brand* duplicates in one category (e.g. Netflix+Hulu) — correctly does not fire for same-brand/different-descriptor duplicates, since that's the merchant-alias problem below, not this detector's job.
- **Merchant-alias merge live** — new `merchant_aliases` table (migration `20260711120000`, RLS user-scoped) + `findMerchantAliasCandidates()`/`resolveAlias()` in `recurringSummary.js` let a user confirm that two differently-worded bank descriptions are the same real payment (bank changed its ACH descriptor). Never auto-merged — surfaced as a "Same payment, different name?" section in Insights (renders only when candidates exist), user taps confirm/reject. `computeRecurringSummary` takes an optional `aliasMap` param, resolved transitively via `resolveAlias` so 3+ descriptor chains merge into one canonical key instead of splitting across hops. Confirmed live in prod: Claude.ai/Anthropic.comca ($40→$20/mo), Turbotenant/Sheviakov/RENT:SHEVIAKOV rent (3 variants → one $2,001.50/mo entry), plus a bonus find via the same detector (Progressive Insurance/Prog Select Ins). Regular Payments total dropped from $5,218.62 to $3,116.63/mo once confirmed — ~$2,100/mo of phantom duplication removed.
- **ai-chat prompt hardened** (`buildSystemPrompt()`) — fixed a real structural conflict where RESPONSE FORMULA's "one concrete action, no options" was overriding REGULAR PAYMENTS' "frame as a question, never a verdict" (model was producing directive tone like "canceling it would be a concrete win"); added debt payoff as an explicit PRIORITY ORDER tier (wins the ONE NEXT ACTION slot over cash_risk when `INTEREST CHARGES THIS MONTH > $0`); clarified PRIORITY ORDER decides content, FINANCIAL STATE TIER decides tone only; extended the "don't force it into every reply" carve-out from cash_risk to warning too; added a top-level CONFLICT RESOLUTION convention for future contradictions between sections. Confirmed live: AI now mentions an active duplicate every time it's asked about that merchant, with question-framing tone, not a verdict.
- **Merchant navigation live** — tap a transaction in Dashboard "Recent Transactions" filters Transactions by that merchant (new `merchantFilter` state, exact-match on `description`, chip to clear — mirrors the existing `catFilter` pattern exactly). Scoped to Dashboard only; Insights.jsx rows intentionally left alone (Subscriptions rows already have the "ask about" search icon as their tap target).
- **`recurringDetector.js` deleted — third independent recurring-payment engine found and migrated.** It fed the Home "Upcoming Charges" carousel and Dashboard's Cash Flow Forecast with a 90-day lookback + keyword allow-list + no `merchant_aliases` awareness — same blind spot as the Claude/Anthropic and rent cases, just silently manifesting as a wrong/lapsed projection date instead of a duplicate. Both consumers now use `getUpcomingCharges()`/`getUpcomingCardPayments()` (recurringSummary.js) — credit card payments got their own function since `RECURRING_EXCLUDE` correctly keeps them out of Subscriptions/Regular Payments (not reconsiderable) but Cash Flow Forecast needs them anyway (real cash out the door). Verified live: forecast's upcoming total went from $962 (old detector's inconsistent amount-clustering) to $673.48 — a more accurate number, not just smaller.
- Also this session: removed 2 dead i18n keys (`markets.pro_only_title/body`), archived BACKLOG #8 (App Store compliance checklist) as 5/7 done, verified against actual code rather than commit history.
- **Month Calendar — two-level redesign live on Home**, replaces the earlier single-level strip. Level 1: full-month 7-col grid (Monday-first), day color = dominant category, intensity = log-scale by day total (`0.25 + 0.75*log(1+total)/log(1+maxTotal)`) — chosen over linear because linear got crushed by one-time lump payments like rent; this design question is resolved, not open. Tap opens Level 2 (bottom sheet), never navigates directly. Level 2 reuses the existing "Other spending breakdown" sheet's exact visual/interaction pattern (not a new component): shows selected day ± 2 neighbors plus full category breakdown. Double-tap-to-drill-in: 1st tap on a different strip day re-centers the sheet, 2nd tap on the already-selected day navigates to Transactions filtered by that date.
- **Three navigation bugs found and fixed before deploy, verified on real data**: (1) `catFilter`/`merchantFilter`/`dateFilter` didn't clear each other, so sequential taps combined as unintended AND filters — all three handlers now null the other two. (2) Category-row tap in the calendar sheet called the same `goToDate` as "View all", so it never actually filtered by category — added `onDayCategoryClick(date, category)` to set both filters. (3) Tapping a neighbor day in the Level 2 strip navigated away instead of switching the sheet — fixed per the double-tap pattern above.
- **Home block order finalized**: Budget bar and Spending by Category (donut) moved to sit immediately before the Calendar — Budget → Donut → Calendar is one logical sequence (spending, general to specific).
- **Future-day intensity added to the calendar grid, on its own scale from past days** — future days previously rendered at a fixed alpha (0x33) regardless of predicted amount, so a $10 and a $2000 upcoming bill looked identical; now uses `dailyIntensityAlpha` against `maxFutureDayTotal` (computed from `futureByDay` only, not shared with past `maxDayTotal`) since past totals are a daily sum across all transactions while future amounts are one dominant merchant's predicted charge — sharing one max (usually set by a lump payment like rent) would have flattened every future day near the floor. Verified on real upcoming charges: $9.99 → 55%, $396.58 → 100%.
- **Scheduled one-off payments live** — new `scheduled_payments` table (user-scoped RLS, `pending`/`completed`/`cancelled` status), added via "+ Add planned payment" in the calendar's Level 2 sheet for a future day. Merges into `getUpcomingChargesByDay` alongside recurring predictions (single source, same color/intensity language) rather than a parallel path. Live "balance left after this" preview reuses `projectBalanceAt`, extracted from `CashFlowForecast` (same formula, parameterized by target date instead of hardcoded end-of-month) — refactored the existing component instead of a second implementation. Cancel/remove button in the sheet (tagged via `scheduledPaymentId`, only present on scheduled items, not recurring projections) flips status to `cancelled`; the existing `status==='pending'` filter already excludes it from grid/forecast, no extra logic needed. Push reminder 3 days ahead added to `push-notify`'s cron batch scan. Verified end-to-end on real account data (DB row → deployed bundle → recomputed alpha/balance).
- **Watchlist star on stock detail screen + limit raised to 20** — `StockDetail` (Overview/Chart/AI/Buy tabs) now has a star toggle reusing Markets.jsx's existing `watchlist`/`addToWatchlist`/`removeFromWatchlist` state (no new table — `profiles.watchlist` JSONB already supported this, it just had no entry point from the stock screen). Toast feedback (existing global `showAlert`) when the cap is hit, since this screen has no visible counter to make a silent no-op safe. Cap raised from 12 to 20 (`MAX_WATCHLIST` constant) after confirming it was a pure UI number, not a Finnhub batching/rate-limit constraint — display switched from a 2-col tile grid to the row-list pattern already used by Portfolio Holdings.
- **Aha-moment interstitial live** — `AhaMoment.jsx`, shown after onboarding's final step only if a bank was connected during that pass (`bankConnected`); "Skip for now" users get the old direct redirect. Up to 3 facts, all reused from `recurringSummary.js` (no new detection logic): duplicate subscriptions → largest regular payment vs income (≥30%) → upcoming charge the balance may not cover, falling back to top spending category over the full synced history when none of those fire (realistic default for thin/Sandbox-style connections, verified not a rare edge case). Verified on 3 real scenarios: rich history (largePayment + cashRisk fired), a 10-day-thin simulation (fallback fired cleanly), and zero transactions (interstitial skipped). Found and fixed on real data: `computeRecurringSummary`'s `.name` is a raw bank descriptor (unlike `getUpcomingCharges`, which already cleans it) — paired the cleaned name with its category so the card stays meaningful even when the descriptor itself is just a bare surname. Fixed only in `AhaMoment.jsx`, not migrated everywhere — see docs/known-issues.md.
- **Onboarding feature-grid icons fixed** — the "here's what Pro unlocks" step used raw emoji (🏦🤖📊📈) instead of the app's icon set; swapped for existing `Icon.jsx` icons (`bank`/`activity`/`award`/`trending-up` — `activity` already had a precedent as "AI Analysis" in Markets.jsx). No new icons needed.
- **Sentry backend rollout history**: scope-isolation between concurrent requests verified live on `ai-chat` (2 parallel test requests → 2 clean, non-leaking events); root cause of the original "events not arriving" symptom was a wrong DSN project ID (copy error at Sentry project creation), not a confirmed SDK/Deno-version incompatibility. Full diagnostic trail in BACKLOG.md "СДЕЛАНО 13/14 июля". Current full-coverage status below.
- **Frontend Sentry — DONE, live-verified end-to-end (2026-07-18).** Five independent, unrelated causes stacked on top of each other, each had to be found and fixed before one event finally reached `arkonomy-web`: (1) `VITE_SENTRY_DSN` was missing from Vercel Production env vars entirely; (2) `release` was unset in `Sentry.init()` — the SDK silently discarded every event (`"Discarded session because of missing or non-string release"`, only visible via a temporary `debug: true` hook) — fixed by exposing Vercel's auto-populated `VERCEL_GIT_COMMIT_SHA` through `vite.config.js`'s `define` as `__RELEASE__`, no Vercel dashboard config needed; (3) `vercel.json`'s CSP `connect-src` never had the Sentry ingest domain (`o4511730858131456.ingest.us.sentry.io`) added when Sentry was integrated — confirmed with a real browser "Refused to connect" message, not just a config diff; (4) after the CSP fix, the already-open tab kept showing the stale violation until a genuinely new browser context (new tab + hard refresh wasn't enough, needed full close/incognito) — not an SW bug (network-first fetch strategy + `skipWaiting`/`clients.claim` were already correct), just ordinary per-document CSP fixation; (5) `throw new Error(...)` typed directly into DevTools Console does not reliably trigger `window.onerror`/Sentry's global handler the way a real runtime error does (Console REPL runs in its own VM context) — final confirmation only came from calling `window.Sentry.captureException()` directly via a temporary `window.Sentry = Sentry` hook. Confirmed live in `arkonomy-web`: Frontend/Chrome 148/Windows, `release: 65704b0fe3af`, `handled: yes`. All temporary diagnostic hooks (`debug: true`, `window.Sentry`) removed, clean build redeployed and verified absent from the live bundle via direct grep. Along the way, also fixed an unrelated CSP bug surfaced in the same `connect-src` review: `fonts.gstatic.com` was blocking the service worker's own font fetches (`sw.js:78`) — added to the same allow-list.
- **Calendar day cells show a signed net-amount line, fully closed after 3 contrast iterations** — under the day number: past/today days show honest net (income − expense, red/green by sign); future days show the dominant predicted charge, always neutral white (not red/green — it's one expected outflow, not a true net, and futureByDay has no income-projection concept to net against). Amounts ≥$1000 collapse to whole thousands ("$2211" → "$2k"). Found and fixed before deploy: the new `getDailyNet` initially excluded both "Transfer"/"Transfers", but `groupExpensesByDay` (drives the same cell's color/intensity) only excludes the singular — real data uses the plural — so the cell's fill and its own text were briefly computed from different totals; fixed by matching `getDailyNet` to `groupExpensesByDay`'s exact (imperfect) filter rather than "correcting" it independently (the inconsistency itself is tracked in docs/known-issues.md). Contrast took 3 real iterations to land: (1) text-shadow + bigger margin — insufficient on saturated backgrounds; (2) a border-accent redesign (dark cell interior, colored ring for intensity) was mocked and rejected — read visibly weaker for "see the month's activity at a glance" on a full-month comparison than keeping the fill; (3) final: near-full-cell dark backdrop (`rgba(0,0,0,0.55)`, inset 3px) under the text, fill/intensity untouched — a fixed dark surface guarantees contrast regardless of what color is underneath, instead of tuning shadow/text color per background combination, which kept failing case-by-case. Confirmed on real device across all flagged cells.
- **Sentry error monitoring — all 19 edge functions now covered** (10 high-priority + 9 medium: `weekly-report`, `push-notify`, `plaid-webhook`, `plaid-get-accounts`, `market-data`, `generate-monthly-report`, `stock-ai-analysis`, `check-bank-connection`, `auth-login`). Same `_shared/sentry.ts` pattern (`initSentry`/`captureAndFlush`) throughout. Live-confirmed with a real event in the `arkonomy-edge-functions` dashboard on 2 of the 9 (`market-data`, `generate-monthly-report`) via a temporary `__sentryTest` body-flag hook (added, triggered with a real user JWT, confirmed, removed, redeployed clean) — the other 7 deployed with the identical established pattern but not individually live-tested. Found along the way: `curl.exe` called from PowerShell silently re-escapes embedded double quotes into `\"` even after removing manual backslashes from the script — this is PowerShell's own native-argument marshalling, not a script bug — switched local test scripts to `Invoke-RestMethod` to avoid the whole class of quoting bugs.
- **Recurring-payment detection consolidated on the `get-insights` side — alias-aware, live-verified.** `supabase/functions/_shared/recurringDetector.ts` fully rewritten: the old 90-day-lookback/keyword-allowlist engine is gone, replaced by a faithful TS port of `computeRecurringSummary`/`getUpcomingCharges` from `src/utils/recurringSummary.js` (calendar-month grouping, `RECURRING_EXCLUDE` block-list, `merchant_aliases`-aware via `resolveAlias`). `get-insights/index.ts` now fetches `merchant_aliases` (`status='confirmed'`) and threads an `aliasMap` into `upcomingBills7d`'s `cash_risk` calculation, the same way the client already does. Verified: code-reviewer ("ship it"), security-auditor (1 LOW finding — `groupTransactionsByMerchant` used a plain object keyed by a bank-controlled description, so a description normalizing to exactly `"constructor"` could collide with `Object.prototype` and 500 that user's request; fixed by switching to a `Map`, mirroring how `aliasMap` was already done), test-runner (build clean before and after the fix). **Live-verified on the real account (2026-07-17, manual check — Chrome extension wasn't connected for automated qa-explorer verification)**: Regular Payments shows the merged rent entry as one line, "Rent:Sheviakov", $2,001.45/mo — not split across the Turbotenant/Sheviakov/RENT:SHEVIAKOV descriptor variants. Turbotenant itself still appears separately in Subscriptions at $4.99/mo — correctly NOT merged, since that's Turbotenant's own legitimate SaaS fee, not a rent duplicate.
- **`push-notify` deliberately NOT migrated to the new detector yet** — still runs its own separate inline `detectUpcoming()` copy, same alias-unaware/90-day-lookback logic as before today. Deferred on purpose: push-notify sends real push notifications directly to users, so a false positive/negative in "payment due in 3 days" has a higher cost than `get-insights`' `cash_risk`, which is just one signal among several. Separate follow-up task, see Next tasks.
- **`market-data` JSON-parsing guard added after a real prod incident (2026-07-17)** — Sentry caught a live `SyntaxError: Unexpected token '<'... is not valid JSON` (an upstream API returned an HTML page instead of JSON). Checked `get_logs`: exactly 1 of ~90+ requests over 12h returned 500, the very next request succeeded — a one-off, not a sustained pattern, which argues against the documented Finnhub rate-limit issue as the cause (that would show clustered failures). **Yahoo Finance flagged as the most likely source — a reasoned hypothesis from process of elimination and its known history of occasionally serving non-JSON pages, not a confirmed fact** (no access to the raw Sentry stack trace to verify the exact line/source). Fix, regardless of which of the three upstream APIs caused it: added `parseJsonSafe()` (reads response as text, parses explicitly, throws a clear source-labeled error instead of letting a native SyntaxError propagate) at all three unguarded `.json()` call sites (Kraken, Yahoo Finance, Finnhub) in `market-data/index.ts`; also hoisted `type`/`symbol` out of the try block so `captureAndFlush` can report which request was being served when it failed — previously that info was invisible to Sentry, forcing exactly the kind of indirect timing/log-based guesswork this incident required.
- **Full RLS audit closed (BACKLOG #13)** — security-auditor (opus) walked all 14 tables' live `pg_tables`/`pg_policies`, not just migrations. Three findings, all fixed and applied live: (1) **HIGH** — `rate_limits`' `check_and_increment_rate_limit` (`SECURITY DEFINER`) was never `REVOKE`d from `PUBLIC`/`anon`/`authenticated` (unlike `login_attempts`' equivalent functions) — any logged-in client could call it directly and burn another user's rate-limit window; fixed with the same `REVOKE`/`GRANT service_role` pattern, verified live via `information_schema.routine_privileges`. (2) **HIGH** — `profiles.alpaca_access_token` (a live Alpaca trading OAuth token) was being SELECTed into the browser purely to compute a boolean; replaced with a parameterless, non-`SECURITY DEFINER` `has_alpaca_token()` RPC that runs under the existing `profiles_owner_select` RLS policy — the token itself no longer reaches the client. (3) **MEDIUM, scope grew mid-investigation** — what started as "the `investments` table has no CREATE TABLE in migrations" turned out to affect **6** tables (`profiles`, `categories`, `savings`, `transactions`, `investments`, `notification_preferences`) — all predate the migrations folder entirely. Closed with 6 separate baseline migrations (`20260412000000`-`20260412000005`), each capturing the real live schema via `information_schema`/`pg_constraint` (not application code — `App.jsx`'s column list was already known to be stale) and deliberately timestamped before the earliest pre-existing migration that touches that table, so a fresh `supabase db reset` replay now actually reproduces production instead of failing partway through. Every one of the 6 was reviewed individually by code-reviewer + security-auditor before applying (not batched), and applied as a verified no-op against the live database each time. Found and logged separately (not fixed, out of scope): `profiles.preferred_language` is a dead read/write path — the column doesn't exist live at all.

- **2026-08-03, three shipped pieces**: (1) Free→Pro funnel audit fixes — `TRIAL_PERKS` no longer claims "Health Score tracking" (free to everyone), category-donut paywall is partial blur (top 3 visible) not full blur, comments added at the 3 Alpaca trial-exclusion gates. (2) **iOS Upgrade CTA restored for US App Store storefront users only** (`cd281c4`) — new `ios/App/App/StorefrontPlugin.swift` (first native plugin in the repo) reads the real StoreKit 2 storefront (not device locale, a legally meaningful distinction per Guideline 3.1.3/the April 2025 Epic v. Apple ruling); `@capacitor/browser` added to open `app.arkonomy.com/upgrade` in Safari, not the in-app WebView; `showRealUpgrade = !IS_IOS_NATIVE || isUSStorefront` replaces the old blanket `!IS_IOS_NATIVE` at all 6 gates. Xcode-side integration (add file to target, `npx cap sync ios`, compile check) still pending — done on Windows, no Mac available this session. (3) **Credit card debt visibility + debt-aware prioritization** (`4698df8`) — new Dashboard "Credit Cards" card (total debt, per-card utilization %, Net Worth line), new `get-insights` `debt_utilization` signal (>30% worst-card utilization) that suppresses `savings_opportunity`/`positive_progress`, ai-chat's PRIORITY ORDER split into an explicit interest-without-cash-risk tier. Plaid Liabilities (real APR/due-date/exact limit) deliberately deferred, see Next tasks.

- **2026-08-06, Dashboard premium-dark redesign — MERGED to main, live in prod (`5d95158`, confirmed via `vercel inspect` against `app.arkonomy.com`, not just push output).** New `DASHBOARD_C` palette (gold `#E8C97D`, ruby `#D64F5E`/`#E4677A` bright variant for inline text, emerald `#2FB37D`, bg `#12161F`, card `#1D2433`) applied across the whole screen — every element, including previously-untouched ones (`UpcomingChargesCard.jsx`, `BottomNav.jsx`'s active-tab color). Restructured to a compact "what matters today" layout: Coach block (top, inline-highlighted numbers), compact calendar week (full month grid unchanged behind "View full month"), Balance/End-of-month, Next up (2-line, icon), Credit Cards (single-card header uses the card's own name, no duplicate total), Spending by Category (donut + side legend, thinner ring), Health Score, compact Markets (S&P 500/Bitcoin only). Monthly Cash Flow and Budget bar moved to Insights.jsx (didn't exist there before — confirmed via grep, not a duplicate); full Markets widget stayed exactly where it already lived (Markets.jsx nav tab, always independent from Dashboard's own widget). Bottom nav reordered to Home/Txns/Coach/Markets/Savings — "Coach" is a rename of the existing Insights tab (same route/content), not a new screen. Before merge, 3 final polish items shipped and user-confirmed live on preview: donut ring thinner (`sw`: 10→7, also grows center-text breathing room since `innerR` derives from `sw`), Credit Cards single-card utilization%/Net Worth split into separate rows (was one merged `space-between` line), BottomNav's decorative active-tab dot removed (not a notification badge — that's the separate `insightCount` badge on the AI button). Also fixed before merge: the CORS allow-list regex on the 5 already-fixed edge functions only matched Vercel's simple per-deployment hash format (`arkonomy-<hash>-...`) — didn't match the git-branch-alias format (`arkonomy-git-<branch-slug>-<hash>-...`, hyphens inside the subdomain), which silently broke login on the branch-alias preview URL; `[a-z0-9]+` → `[a-z0-9-]+` in all 5, still anchored to the exact `shevvik88-dots-projects.vercel.app` suffix, not a wildcard. Origin branch: `dashboard-redesign-premium-dark` (was live-verified there via its stable git-alias preview URL before merge).
- **2026-08-06, 5 of 23 edge-function CORS configs fixed.** Found while testing the redesign on a Vercel preview: `auth-login`, `check-bank-connection`, `market-data`, `plaid-get-accounts`, `get-insights` all had `Access-Control-Allow-Origin` hardcoded to a single static origin (`APP_URL` or the literal prod URL) — works for prod, silently CORS-blocks any preview/staging origin. Replaced with an allow-list/regex match (prod, or any `arkonomy-*-shevvik88-dots-projects.vercel.app` preview) that echoes the request's own origin only on a match, never a wildcard, falling back to prod otherwise — each live-verified via real preflight requests against prod/preview/untrusted origins before deploying. **18 functions (17 + `_shared/rateLimit.ts`) still have the old pattern** — see BACKLOG.md tech-debt entry for the full list; fix one at a time, same live-verification standard, not batched.
- **2026-08-06, CI concurrency fix.** `codeql.yml`/`semgrep.yml`/`gitleaks.yml` had no `concurrency` group, so rapid successive pushes to `main` (6 in one session) queued more workflow runs than available runner capacity, silently cancelling some (CodeQL specifically, the slowest of the three). Added a standard `group: ${{ github.workflow }}-${{ github.ref }}, cancel-in-progress: true` block to all three. Pure config change. One residual gap: the push that shipped this fix (`e0a7acb`) landed during a real, unrelated GitHub-wide Actions outage (confirmed via githubstatus.com, not guessed) and never got a run at all, even after GitHub reported recovery — not auto-replayed. Not manually re-run; will be swept up by the next push to `main`.

- **2026-08-08, Today's Lesson functionality — DONE, live in prod (`54c3850`).** Replaces the old non-interactive "Coming soon" placeholder row. New `lesson_streaks` table (migration `20260808000000`, user-scoped RLS select/insert/update own row only). `src/utils/lessons.js` — 12 curated lessons (English only for v1, see Known issues), `getTodaysLesson()` picks deterministically by day-of-year (same lesson for everyone, no DB round-trip for content), pure `computeNextStreak()` for the continue/reset/no-op transitions, `getPersonalizedLessonNote()` reuses data the caller already computed (cashPositionLow / nearest upcoming charge / top category) for one relevant real-data sentence — same static-template pattern as `AhaMoment.jsx`, deliberately not a live LLM call on every open (no new AI cost, no unreviewed-financial-advice risk on a daily-habit surface). Free for all users (same precedent as Health Score). Tap-to-open = completion, bottom sheet UI (same pattern as Cash Flow Forecast/Credit Cards/Upcoming Charges — `C`-palette chrome, not `DASHBOARD_C`, matching how every other Dashboard sheet is actually styled). **Streak logic live-verified against real DB before commit, not just code review**: ran the actual RLS-protected REST path (real access token, same request `completeLesson()` makes) three times, with `last_completed_date` manipulated via service-role SQL between runs to simulate time passing — confirmed all 3 transitions on real rows, independently re-checked via direct SQL each time (not inferred from script output alone): first open → `streak:1`; `last_completed_date` set to yesterday, opened again → `streak:2` (continues); `last_completed_date` set to 3 days back (missed day), opened again → `streak:1` (resets, doesn't keep growing). Test row deleted from the account afterward.

- **2026-08-09, Sentry `UpstreamUnavailableError` spike (2→42/week, "Escalating") — diagnosed and a likely amplifier fixed; outcome not yet confirmable.** No Sentry MCP access this session, so the exact 42-event HTTP-status breakdown (`upstream_status` tag) couldn't be pulled directly — diagnosis instead went through code + live Supabase edge-function logs. Found: Dashboard's `MiniMarkets` widget (part of the 2026-08-06 redesign) polls `market-data`'s `overview` type every 60s per open tab (`setInterval`, `Dashboard.jsx:1122`, deliberately left unchanged — the client-side polling itself isn't the problem) → `getMarketSnapshot()` (`_shared/marketSnapshot.ts`) → 4 separate Finnhub calls (SPY/QQQ/BTC/ETH) every tick. That file's own comment already said results are "not user-specific... fetch once and reuse it" — the widget was doing the opposite, every open tab independently re-hitting the single shared 60 req/min Finnhub key. Live logs showed ~44 `market-data` POSTs in ~40 minutes (~66/hr), well above what a single Markets-tab visit would produce. Note: `overview`'s own failures are swallowed by `Promise.allSettled` inside `getMarketSnapshot` and never throw `UpstreamUnavailableError` — so the fix targets the shared Finnhub *budget* pressure this polling adds (which plausibly increases failure odds on the `quote`/`chart`/`stats` calls that DO throw that error), not a direct one-to-one cause proven from Sentry data. **Fix (`63168bf`, deployed to both `market-data` and `weekly-report`, the two callers)**: in-memory cache inside `getMarketSnapshot()`, 45s TTL, shared across every caller of the warm isolate (not per-user) + in-flight-promise dedup so concurrent cache-miss callers collapse into one real Finnhub round-trip. `getMarketSnapshot()`'s signature is unchanged. **Whether this actually drops the error rate needs the next weekly Sentry report — user will forward the new `UpstreamUnavailableError` count when it arrives, compare against the 42/week baseline then, not before.**

- **2026-08-09, long-press-any-card AI hints — DONE, live in prod (`dc84713`).** Spec decided in a separate chat session (not this one), relayed and confirmed here before implementation. Long-press on any of the 6 interactive Dashboard cards (Coach block, Credit Cards, Spending by Category, Health Score, Cash Flow, Next up) opens AI chat with a prefilled, card-relevant question already sent — `src/hooks/useLongPress.js` (Pointer Events, no new dependency, 500ms threshold, 10px move-cancel) + `src/utils/cardQuestions.js` (`getCardQuestion(cardKey, data, t)`, same static-template + real-data-substitution pattern as Today's Lesson's `getPersonalizedLessonNote`, no AI call to generate the question). New `App.jsx` `openChatWithMessage(msg)` reuses the exact mechanism `Insights.jsx`'s "Ask about subscription" already established. Floating AI chat button in `BottomNav.jsx` now hidden only when `screen === 'dashboard'` (duplicated the "Ask your coach anything" row there) — unchanged, badge included, on every other screen. **Found and fixed before it could ship broken**: 2 onboarding tour steps (main `TUTORIAL_STEPS` step 8, `MINI_TOURS.invest` step 3) targeted the now-Dashboard-hidden `[data-tutorial="ai-chat"]` button and both actually run while `screen==='dashboard'` (steps with `screen: null` don't force-navigate, inheriting whatever the last `screen:"dashboard"` step left it on) — retargeted both to a new `[data-tutorial="ask-coach"]` attribute on the still-present "Ask your coach anything" row. Live-verified the floating-icon fix specifically is in the served bundle (grepped the actual prod JS for post-fix markers, not just trusted the deploy step) after the user initially reported still seeing it — confirmed stale client cache, not a real regression, same class of issue as the 2026-07-12 calendar-intensity false alarm.

- **2026-08-09, Dashboard palette polish pass — DONE, live in prod (`ebb6e48`).** Four items: (1) `CAT_COLORS` desaturated — the original bright Tailwind-400 set read as "rainbow"/screaming against `DASHBOARD_C`; HSL remap (S→35+S*0.22, L→L*0.92, hue unchanged) computed against the actual `DASHBOARD_C` accent trio's own S/L range, not picked by eye. Government/Other/Transfer (already desaturated slate greys) left untouched. (2) Two label-color typos fixed — "Today's lesson" row title and "Spending by Category" title were `DC.text` (near-white) instead of `DC.muted`, the token every other Dashboard label already used consistently (confirmed by grep across all uppercase-eyebrow labels first — this was 2 isolated typos, not a real palette gap). (3) `getPersonalizedLessonNote()`'s `cashPositionLow` branch (`lessons.js`) hardcoded "Today's lesson on cash flow forecasting" — false whenever the actual lesson (independent day-of-year rotation) isn't that one, as reported live against a real "Avalanche vs. snowball" mismatch; reworded to the same neutral "see if today's lesson connects" phrasing the other two branches already use. (4) **Found and fixed along the way, not part of the original ask**: 2 e2e tests broken by the long-press feature above — both explicitly navigate to Dashboard then click the now-hidden `[data-tutorial="ai-chat"]`. `ph-mask-verification.spec.js`'s Chat test was hard-failing; `new-user-journey.spec.js`'s "AI Assistant" step was silently swallowed by its own try/catch and never actually testing chat despite the suite reporting an overall pass — the kind of false-green result flagged elsewhere in this file. Both retargeted to `[data-tutorial="ask-coach"]`, verified by confirming the real "✅ Chat opened" log line reappears instead of being silently caught.

- **2026-08-11, ai-chat LANGUAGE default fix — DONE, live in prod (`e9d39b1`, ai-chat v105).** `App.jsx` now sends `i18n.language` into ai-chat's `financialContext`; the prompt's LANGUAGE section uses it as the default reply language instead of inferring purely from message text (was breaking after a mid-session UI language switch, since sessionStorage chat history can carry earlier-language messages). Took 3 rounds of live-verified diagnosis before it actually worked — logged in full because each ruled-out hypothesis is a real, reusable lesson: (1) first attempt's exception clause ("switch if the message is in a different language than app language") was logically self-canceling — identical trigger condition to the default case itself, so a RU-app + plain-English-message call kept losing to English; (2) fixed the wording to be asymmetric (English message text alone no longer counts as override-worthy; a specific non-English language still does) — didn't fully fix it; (3) checked the test itself wasn't the confound — it was, partially: the test never supplied `financialContext.metrics`, so every call hit the hardcoded English "balance unavailable" sentence in DATA_BLOCK regardless of language, independent of the actual bug — fixed the test, RU-default still failed; (4) checked `temperature` — was unset (API default 1.0, high sampling variance) — set to 0.4, still failed alone; (5) actual fix was the wording change (2) **plus** a language+content-fused directive placed at the very start of DATA_BLOCK ("describe the financial facts below in {languageName}"), right before the actual numbers, reinforcing LANGUAGE at the point of generation instead of relying on an instruction stated ~150 lines earlier in a long prompt. Verified 4/4 via `scripts/test-ai-chat-language.mjs` (RU-default, RU-override-over-EN-app, ES-default, EN-baseline) — user ran it live each round (no access token in chat, per privacy rule), each edge-function version independently confirmed live via `mcp__supabase__get_edge_function` before asking for a re-test, not just trusting the deploy command's own "success" output.

- **2026-08-15 through 2026-08-18, security-hardening pass — DONE, live in prod, most-critical findings closed.** Two new living documents: `SECURITY_THREAT_MODEL.md` (STRIDE analysis of the whole architecture, kept updated as findings close) and `PENETRATION_TEST_PLAN.md` (structured requirements + reproducible test scenarios + a status table, most rows still "not yet tested" — this was one pass, not a completed audit).
  - **CI layer**: Gitleaks official-action (incremental scan on push/PR + a daily full-history scan via schedule/workflow_dispatch), CodeQL, Semgrep, and Dependency Review all active; `security-guidance` Claude Code plugin installed.
  - **6 CodeQL findings closed**: `App.jsx` sessionStorage clear-text storage — accepted risk, documented (code comment + this file's Storage conventions section); `delete-account` stack-trace leak — fixed; 4× incomplete URL sanitization in `sw.js`/`qa-test.cjs` — fixed via a shared `isTrustedHost()` helper (hostname match via `new URL()`, not `.includes()`); dead root-level `sw.js` (stale `CACHE_NAME: arkonomy-v2` copy, `public/sw.js` is the only one actually served) deleted.
  - **2 Dependabot alerts closed**: `brace-expansion` (CVE-2026-69152) and `nanoid` (GHSA-2v37-7h3g-55p8) — both verified line-by-line unreachable via the actual runtime call path (not assumed), then pinned via `package.json` `overrides` anyway for defense-in-depth.
  - **Historical service_role JWT leak** (hardcoded in a migration file, commit from April 12) — confirmed empirically dead via a live API call against the actual leaked key (not just trusting the Supabase Dashboard's "Revoked" label), exposure window ~13 hours, not the 4 months between leak and this check.
  - **IDOR audit, all 23 edge functions + frontend direct-table CRUD** — 1 real finding (`savings_reminders.goal_id` had no FK-ownership check, exploitable via `push-notify`'s service-role read bypassing RLS) fixed with a `WITH CHECK` `EXISTS` clause on the RLS policy, live-verified with a real cross-user upsert attempt (`42501` rejection).
  - **Race-condition audit, 5 findings closed**: Stripe webhook idempotency (`event_id` PK table, all 5 event types), Alpaca double-order (atomic pending-row insert + `UNIQUE` constraint, replacing a racy SELECT-then-INSERT check), Stripe checkout race (`checkout_pending_at` atomic check-and-set, complementary to the existing `findActiveSubscription` guard), Plaid cursor compare-and-swap (concurrent manual-sync + webhook-triggered sync no longer let a stale cursor overwrite a more-advanced one), Savings lost-update pattern (documented as accepted risk — currently dead code, no UI path calls it).
  - **Delete-account race chain (HIGH severity, found via the pentest plan's business-logic section)** — deleting an account while an Alpaca order or Stripe checkout was still completing let the later confirm-UPDATE match 0 rows completely silently (no exception, no Sentry event) while real money had already moved. Closed: rowcount-check + Sentry alert on both confirm-UPDATEs; `delete-account` now polls for a pending Alpaca investment (1s interval, ~7s max) and blocks with a 409 if one doesn't resolve; `profiles.checkout_session_id` tracks the live Stripe Checkout Session so `delete-account` can actively call `stripe.checkout.sessions.expire()` instead of just hoping the up-to-24h completable window passes uneventfully. **Fail-open hotfix**: the pending-Alpaca-investment check's own Supabase query error was originally left undestructured, so a query failure silently read as "no pending investment" and let deletion proceed anyway — fixed to fail closed (500 + Sentry alert) on the final retry attempt instead.
  - **`transactions.amount`/`savings.target`/`savings.current` CHECK constraints added** — direct RLS-scoped inserts of negative, zero, and Postgres's special `NaN` numeric value all previously succeeded silently; `NaN` is arithmetically contagious and would have silently corrupted that user's own computed totals (Health Score, Cash Flow Forecast, get-insights). Verified against real prod data before applying (0 violations after deleting one identified QA test artifact); sign convention (amount always positive, `type` encodes direction) confirmed from `sumAmounts()`'s own code comment, which already documented this exact bug class having happened once before. Found and fixed the same inconsistency live in `App.jsx`'s category-spending grouping (this month wrapped in `Math.abs()`, last month didn't) by routing both through `sumAmounts()` instead of two separate hand-rolled reduces.
  - **Deferred to future sessions** (not started, or only partially covered by the one `PENETRATION_TEST_PLAN.md` pass): broader adversarial audit (business-logic flaws, multi-step attack chains beyond the ones already tested), auth/session security in depth (rate-limit key composition beyond the email+IP check already done, refresh token rotation, concurrent-session detection), a full SSRF audit across all 23 edge functions (only Alpaca redirect_uri and market-data's symbol param were checked), integration tests for today's 4 race-condition fixes, and a written incident-response runbook.

- **2026-08-19, PENETRATION_TEST_PLAN.md 3.x business-logic section fully closed.** 3.1/3.2 (amount edge values, server-side limit enforcement in `alpaca-invest`) tested live, both pass as designed. 3.5 (malformed JSON → 500 instead of 400 in `alpaca-invest`, found via 3.1) fixed same day, live-verified. 3.6 (same unguarded `req.json()` shape found in 4 more functions — `ai-chat`, `auth-login`, `plaid-exchange-token`, `stock-ai-analysis`) fixed and deployed same day (`ai-chat` v106→v107, `auth-login` v43→v44, `plaid-exchange-token` v59→v60, `stock-ai-analysis` v49→v50), live-verified via `scripts/test-malformed-json-3.6.mjs` — 4/4 return 400, not 500. 3.3 (delete-account race chain, originally found/fixed 2026-08-17) and 3.4 (missing CHECK constraints, originally found/fixed 2026-08-17) re-verified live against the shipped fixes — both confirmed still closed, plan table updated to match (it had gone stale, still reading "Not fixed" two days after the actual fix shipped).

- **2026-08-24, Alpaca Portfolio UX gaps closed — Holdings scale, Pending Orders, StockDetail owned position (PR #34, #35→#38, commits `14139f4`/`3adc139`).** Investigated a real UX complaint ("bought a stock, can't tell what happened") — two separate, unrelated causes, not one bug. (1) WST/SPCX orders were correctly still `accepted` — day order, zero trading sessions since a Friday-night submission, verified against Alpaca's own `/v2/calendar`/`/v2/clock`, not a bug (an initial "critical bug" conclusion was drawn, then explicitly retracted after re-checking the calendar) — but were completely invisible anywhere in the app. `alpaca-portfolio` now also fetches `/v2/orders?status=open`, new "Pending Orders" section. (2) Clicking a Holdings row already opened `StockDetail` (worked the same from Watchlist/Trending/Search) but threw away `avg_entry_price`/`qty`/unrealized P&L on the way — new `ownedPosition` prop, "Your Position" card. Bonus fix found along the way: `StockDetail`'s header used a static ~18-symbol curated map for company name — any other ticker (including every one actually bought: MP/WST/SPCX) showed the bare symbol twice, no real name; now uses live `stats.name`. security-auditor found and fixed 2 real bugs in the `alpaca-portfolio` diff before commit: a `notional:null` order (qty-based, e.g. placed via Alpaca's own app) rendered a fabricated "$0.00" instead of the real share count; order side was hardcoded "Buy" ignoring the real `side` field. **Process lesson**: PR #35 was deliberately opened stacked on PR #34's own branch (not `main`), but merging a PR whose base is a feature branch only advances that branch — it does not also merge into `main`. That gap sat unmerged until a separate completion PR (#38) closed it. Worth remembering for any future stacked-PR chain: closing the stack needs an explicit final branch→main PR, GitHub doesn't do it automatically.
- **2026-08-24, Watchlist + Pending Orders capped at top-3 + "+N more" (part of PR #35/#38).** Both lists had no cap — Watchlist grows to `MAX_WATCHLIST=20`, Pending Orders (new the same day) had no limit at all. Same pattern as Holdings/Dashboard's credit-cards list. Watchlist's read-only view deliberately keeps the user's own drag-order (not re-sorted alphabetically/by recency) — edit mode stays fully uncapped since that's the dedicated manage/reorder/remove view. Pending Orders sorted by `submitted_at` desc (most recent first).
- **2026-08-24, Asset Allocation "Stocks" tile fixed — real architectural gap, not a "haven't invested yet" appearance (PR #39, `6d8882f`).** `Savings.jsx`'s Stocks tile read only Plaid investment-type accounts — Alpaca holdings (round-up buys, manual buys from Markets.jsx) never fed into it at all, so it stayed at $0/0% no matter how much was actually invested; the two portfolios (Alpaca via `alpaca-portfolio`, Plaid via `plaid-get-accounts`) were fully disconnected data sources on two different screens. Fix: `investTotal` = Plaid investment-account balances + sum of `market_value` across Alpaca positions — deliberately not `portfolio_value`, which folds in uninvested cash sitting in the brokerage account and would have wrongly inflated "Stocks". Double-count guard added (excludes any Plaid investment account whose `institution_name` matches Alpaca) for the edge case where the same brokerage is ever linked both via Plaid and via this app's own direct OAuth connection. Live-verified on the real account: Stocks went from $0.00/0% to real values ($5.14, later $15.16 once more pending orders filled) as the session progressed.
- **2026-08-24, round-up investing asset selection — Spare Change Investing no longer hardcoded to SPY (PR #45, `8ef2f02`).** `App.jsx`'s `investAlpaca()` and `Savings.jsx`'s confirmation sheet both had a literal `"SPY"` with no way for a user to choose otherwise, on a totally separate code path from Markets.jsx's own user-chosen-ticker Buy flow. New `profiles.roundup_symbol` (migration `20260824010000_roundup_symbol.sql`, `TEXT NOT NULL DEFAULT 'SPY'`, `CHECK ^[A-Z]{1,5}$` mirroring `alpaca-invest`'s own client-side regex) — existing rows backfilled to `'SPY'` automatically via the column DEFAULT at migration time, confirmed live (17/17 rows), zero action needed from current users. `profiles` has column-level GRANT allow-lists from the earlier live `plan`/`stripe_customer_id` exploit (`20260730000000`/`...0001`) — both the SELECT and UPDATE allow-lists had to be re-issued in full with `roundup_symbol` added (GRANT is not additive per-column in Postgres); the full column lists were verified directly against the live DB grants right before writing the migration, not copied from the prior migration files, and re-verified after applying that nothing else was narrowed (SELECT 19→20, UPDATE 7→8 columns, exact diff). StockDetail (Markets.jsx) gained a "Set as round-up investment" button in the Buy tab, gated the same as the existing Buy button (`!isPro || isTrial`) — reuses the existing click-any-ticker-anywhere flow (Holdings/Watchlist/Trending/Search all already open this same screen) instead of building a second picker inside Savings. `Savings.jsx`'s confirmation sheet shows the real `profile.roundup_symbol` instead of the hardcoded "SPDR S&P 500 ETF (SPY)" label, plus a "Change" link routing back to Markets (`openMarket()`, already existed for other deep-links) — deliberately ticker-only, no company name fetched, not worth a second market-data call for a cosmetic detail. Live-verified end-to-end without placing a real order: set MP as the round-up symbol on StockDetail → confirmed directly in the DB (`roundup_symbol: "MP"`, not just trusting the UI) → confirmed Savings' confirmation card renders "MP". Both the security-auditor and test-runner subagents hit the account's monthly spend limit mid-run this session — self-reviewed directly instead of retrying (build clean, ESLint 0 new errors, 4 e2e tests pass).
- **2026-08-24, Dependabot batch — 5 merged, 5 reviewed and cleared but still awaiting manual merge.** Merged: #25-29 (`@capacitor/core`→8.5.0, `@typescript-eslint/parser`→8.67.0, `eslint` 9.39.5→10.8.1, `playwright`/`@playwright/test`→1.62.1). Reviewed individually — real `npm ci`/`npm run build`/`npx eslint` run per branch, not a batched assumption — all clean, **not yet merged** (no merge permission via the GitHub API token, same limitation hit repeatedly this session): #40-44 (`@capacitor/android`→8.5.0, `eslint-import-resolver-node`→0.4.0, `eslint`→10.9.0, `firebase`→12.18.0, `supabase` CLI→2.115.0). The eslint 9→10 jump required migrating off `eslint-plugin-import` first (PR #37, `cdb9812`) — the old plugin's peer-deps cap at eslint `^9` and it never got a v10-compatible release — to `eslint-plugin-import-x`; the new resolver package (`eslint-import-resolver-node`) had to be added as an explicit devDependency since it's only an *optional* peer of `import-x`, not a transitive dependency the way it was before (silently losing it would have reintroduced an exact false-negative already documented in `eslint.config.js`'s own comments). Live-verified with a deliberately planted violation (a temporary bad import, Profile.jsx→Markets.jsx) — caught correctly, then reverted, diff confirmed empty. **`supabase` in #44 is the CLI** (`apps/cli`, devDependency for `functions deploy`/local dev), **not `@supabase/supabase-js`** (the runtime client SDK actually used for auth/RLS queries) — worth remembering the next time a "supabase" version bump looks scarier than it is; the two are easy to conflate by name. CLI 2.115's real breaking changes (`db diff`/`db pull` default to a new schema-diff engine, `test db` now fails on 0 tests) have zero automated blast radius here — confirmed no CI workflow, `package.json` script, or git hook invokes any of the affected subcommands; `supabase functions deploy` remains the only CLI usage, and it's a manual command a person runs, unaffected by either change.
- **2026-08-24, stray git worktree removed.** `.claude/worktrees/agent-a0219c24bf7ade1f7` — a registered git worktree left over from a much earlier session (the same one previously known to have needed a `storageState.json` cleanup; the directory itself survived that cleanup). Its stale `eslint.config.js` still referenced the now-removed `eslint-plugin-import`, throwing a module-not-found error whenever `npx eslint .` ran unscoped — harmless noise, not a real regression, but confusing. Contents reviewed before deleting, not assumed safe: no real credentials found (one leftover screenshot showed a masked password field for a synthetic `qa-audit-...@arkonomy.com` test account, not a leak; two other "token" hits were just legitimate already-committed migration filenames). Removed via `git worktree remove --force` (not a raw `rm -rf`, to avoid leaving dangling `.git/worktrees/` metadata); also found and deleted an orphaned local branch (`worktree-agent-a0219c24bf7ade1f7`) pointing at a commit already merged into `main` since July 29 — nothing lost.

## Known-issue resolutions folded into the log above (kept for searchability)
- Resolved 2026-07-17: both Deno-side recurring-payment engines now use the shared alias-aware detector, and the client-side prototype-collision pattern is fixed too. `push-notify/index.ts` migrated to `_shared/recurringDetector.ts`'s `getUpcomingCharges` (own inline `detectUpcoming()` removed entirely); `maxResults: Infinity`, exact `daysUntil === NOTIFY_DAYS_AHEAD` filtering, and title-cased merchant names preserved deliberately at the call site (code-reviewer verified all three). Live-verified on a real account: Rent:Sheviakov merges to one line; the $2,000 vs get-insights/UI's $2,001.45 difference was confirmed via direct SQL query as an expected consequence of push-notify's 90-day lookback (deliberately not widened this pass) excluding older $2,002/mo charges, not a merge bug. `src/utils/recurringSummary.js`'s `groupTransactionsByMerchant` (the client-side original the Deno file was ported from) also switched `{}` → `Map` — same fix, mirrored back.
